{
  "name": "@gto-self/adaptive-core",
  "version": "0.0.0",
  "private": true,
  "description": "Composition layer: REFERENCE baseline + opponent adjustment profile -> ADAPTIVE. Never a source of poker rules or player facts.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project adaptive-core"
  },
  "dependencies": {
    "@gto-self/player-core": "workspace:*",
    "@gto-self/shared": "workspace:*",
    "@gto-self/strategy-core": "workspace:*"
  }
}
