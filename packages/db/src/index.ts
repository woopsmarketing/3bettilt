/**
 * Drizzle + SQLite persistence (Phase 3).
 *
 * Rules: storage only. No poker rules, no strategy decisions, no derived game
 * state that the engine should own.
 *
 * Phase 0 placeholder — replaced by the implementing phase.
 */
export const DB_PLACEHOLDER = 'db' as const;
