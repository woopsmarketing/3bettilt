{
  "name": "@gto-self/poker-core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure deterministic NLHE state engine. No React, no DB, no GTO.",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project poker-core"
  },
  "dependencies": {
    "@gto-self/shared": "workspace:*",
    "zod": "^4.4.3"
  }
}
