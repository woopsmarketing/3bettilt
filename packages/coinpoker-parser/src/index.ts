/**
 * CoinPoker hand-history text -> neutral domain events (Phase 11).
 *
 * Rules: no UI coupling. Hand-history opponent ids are NOT player identity.
 *
 * Phase 0 placeholder — replaced by the implementing phase.
 */
export const COINPOKER_PARSER_PLACEHOLDER = 'coinpoker-parser' as const;
