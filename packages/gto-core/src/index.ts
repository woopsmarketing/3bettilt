/**
 * Solution models, provider interface and strategy policy (Phases 9-10).
 *
 * Rules: knows nothing about individual players. Never fabricates strategy
 * numbers. Mock data must be labelled at the type level.
 *
 * Phase 0 placeholder — replaced by the implementing phase.
 */
export const GTO_CORE_PLACEHOLDER = 'gto-core' as const;
