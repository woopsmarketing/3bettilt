/**
 * Players, manual HUD snapshots, observations and confidence (Phase 3).
 *
 * Rules: player statistics never mutate GTO baseline data. Manual CoinPoker
 * HUD snapshots and our own observations stay separable forever.
 *
 * Phase 0 placeholder — replaced by the implementing phase.
 */
export const PLAYER_CORE_PLACEHOLDER = 'player-core' as const;
