{
  "name": "@gto-self/player-core",
  "version": "0.0.0",
  "private": true,
  "description": "Player profiles, manual HUD snapshots, observations, context-scoped confidence.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project player-core"
  },
  "dependencies": {
    "@gto-self/shared": "workspace:*",
    "zod": "^4.4.3"
  }
}
