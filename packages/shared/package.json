{
  "name": "@gto-self/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-cutting primitives: fixed-point money (milliBB), cards, ids, result types.",
  "type": "module",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project shared"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
