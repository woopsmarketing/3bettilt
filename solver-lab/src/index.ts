/**
 * Solver research sandbox (Phase 13).
 *
 * Nothing here is part of the application build. Experiments land here first —
 * CFR on Kuhn/Leduc before anything is claimed about no-limit Hold'em.
 * See docs/GTO_BASELINE.md for the rules that govern this directory.
 */
export const SOLVER_LAB_PLACEHOLDER = 'phase-13' as const;
